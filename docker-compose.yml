version: "3.8"
services:
  initdb:
    image: apache/airflow:2.0.0b3-python3.7
    command: db init
    volumes:
      - ./airflow-home:/opt/airflow
  webserver:
    image: apache/airflow:2.0.0b3-python3.7
    command: webserver
    deploy:
      replicas: 1
    env_file: ./airflow-home/common.env
    ports:
      - 8080:8080
    volumes:
      - ./airflow-home:/opt/airflow
    depends_on:
      - initdb
  scheduler:
    image: apache/airflow:2.0.0b3-python3.7
    command: scheduler
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
    env_file: ./airflow-home/common.env
    volumes:
      - ./airflow-home:/opt/airflow
    depends_on:
      - initdb
